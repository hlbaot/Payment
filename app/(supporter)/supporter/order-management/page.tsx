import SupporterPlaceholder from '@/components/supporter/SupporterPlaceholder';

export default function SupporterOrderManagementPage() {
  return (
    <SupporterPlaceholder
      title="Support Order Management"
      description="Open order-linked support issues, verify order history, and coordinate with operations when customers report settlement delays."
    />
  );
}
